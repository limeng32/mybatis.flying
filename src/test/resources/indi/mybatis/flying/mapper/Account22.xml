<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indi.mybatis.flying.mapper.Account22Mapper">
	<cache ${mybatis.cache} />

	<select id="selectWithoutCache" resultMap="result" useCache="false">
		flying:{"action":"select#{?}", "ignore":"noPassword"}
	</select>

	<select id="select" resultMap="result">
		flying#{?}:{"action":"select", "ignore":"noPassword"}
	</select>

	<select id="selectAll" resultMap="result">
		flying:{"action":"selectAll", "ignore":"noPassword"}
	</select>

	<select id="selectOne" resultMap="result">
		flying:{"action":"selectOne", "ignore":"noPassword"}
	</select>

	<select id="count" resultType="int">
		flying:{"action":"count"}
	</select>

	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		flying:{"action":"insert"}
	</insert>

	<update id="update">
		flying:{"action":"update"}
	</update>

	<update id="updatePersistent">
		flying:{"action":"updatePersistent"}
	</update>

	<delete id="delete">
		flying:{"action":"delete"}
	</delete>

	<resultMap id="result" type="Account22" autoMapping="true">
		<id property="id" column="id" />
		<association property="role" javaType="Role2_"
			select="indi.mybatis.flying.mapper2.Role2Mapper.selectWithoutCache" column="role_id" />
	</resultMap>
</mapper>
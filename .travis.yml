language: java
sudo: false
jdk:
  - oraclejdk7
script: 
        - mvn clean test -DCACHE=true -Dtest=CacheTest -B
        - mvn clean test -B
        - mvn clean test -DCACHE=true -Dtest=CacheTest -P mybatis-331 -B
        - mvn clean test -P mybatis-331 -B
        - mvn jacoco:report
        - pip install --user codecov
install: 
        mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B       
after_success: 
  - codecov
branches: 
  only:
    - ForMybatis3.3.x